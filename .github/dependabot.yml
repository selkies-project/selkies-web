# The site has no runtime dependencies: package.json carries scripts alone, so
# the deploy workflow's actions are the only pinned versions in the repository.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
